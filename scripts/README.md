# LeKiwi Scripts

Three modes for different use cases:

- **`tests/`** - Test individual components directly on host
- **`operate/`** - Remote control & recording (requires host running)
- **`autopilot/`** - Autonomous mode with optional Rerun visualization

See README.md in each directory for commands.

